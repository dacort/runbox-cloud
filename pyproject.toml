[project]
name = "cloud-run"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.38.8",
    "boto3-stubs>=1.39.4",
    "click>=8.1.8",
    "pyyaml>=6.0.2",
    "pyssm-client>=0.1.1",
    "rich>=14.1.0",
]

[tool.uv.sources]
pyssm-client = { path = "../python-session-manager-plugin", editable = true }
